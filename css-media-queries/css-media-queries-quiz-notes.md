# css-media-queries-quiz-notes

## Quiz Questions

After completing this exercise, you should be able to discuss or answer the following questions:

- Give two examples of media features that you can query in an `@media` rule.
  @media rule is used to apply different styles based on the characteristics of the device or screen displaying the content. Here are two examples of media features that can be queried in an @media rule:
- Which HTML meta tag is used in mobile-responsive web pages?

## Notes

All student notes should be written here.

How to write `Code Examples` in markdown

for JS:

```javascript
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
